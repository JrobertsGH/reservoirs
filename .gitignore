# Python
__pycache__/
*.pyc
*.egg-info/
.pytest_cache/
build/
dist/

# Large source/derived data - not committed, regenerated by the pipeline
# (final maps/GIS deliverables in outputs/ ARE tracked - that's the EAP work product)
dams/*/data/
dams/*/ras_project/

# Local env
.env

# Scratch commit-message files used when committing via `git commit -F`
.git-commit-msg.txt
